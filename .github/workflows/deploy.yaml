name: deploy to EC2

on:
  push:
    branches:
      - cicd

jobs:
  deploy 1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a Linux command
        run: ls -la

        deploy 2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a Linux command
        run: cat index.html

